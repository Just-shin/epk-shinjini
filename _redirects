# Netlify redirects configuration
# Proxy all /api/* requests to the backend (Render)
/api/*  https://shinjini-epk.onrender.com/api/:splat  200

# Single Page App (SPA) fallback: serve index.html for non-file routes
/*    /index.html   200
